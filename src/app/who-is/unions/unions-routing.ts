import { Routes } from '@angular/router';
import { UnionsListComponent } from './unions-list/unions-list.component';

export const UNIONS_ROUTES: Routes = [
    {
        path: 'lista-de-sindicatos',
        component: UnionsListComponent
    }
];
